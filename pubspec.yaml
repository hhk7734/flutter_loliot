name: loliot
description: IoT Project

publish_to: "none"

version: 0.1.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^1.2.5
  firebase_auth: ^0.18.4+1
  firebase_core: ^0.5.3
  flutter_bloc: ^6.1.1
  font_awesome_flutter: ^8.11.0
  formz: ^0.3.2
  github_sign_in: ^0.0.3
  google_sign_in: ^4.5.6
  json_annotation: ^3.1.1
  meta: ^1.2.4
  react_grid_view:
    path: packages/react_grid_view
  shared_preferences: ^0.5.12+4

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^1.10.12
  json_serializable: ^3.5.1

flutter:
  uses-material-design: true
